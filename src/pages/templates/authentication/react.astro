---
import { Icon } from 'webcoreui/astro'

import { getAuthenticationTemplateData } from '@data'

import Authentication, { type Props as AuthenticationProps } from '@templates/Authentication/Authentication.tsx'
---

<Authentication {...getAuthenticationTemplateData() as AuthenticationProps}>
    <a
        slot="insideMenu"
        href="https://github.com/Frontendland/webcoreui"
        target="_blank"
        rel="noreferrer"
    >
        <Icon type="github" size={18} />
    </a>
</Authentication>
